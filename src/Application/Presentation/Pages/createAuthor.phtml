<!DOCTYPE html>
<html lang="en">
<head>
    <title>Author Create</title>
    <link rel="stylesheet" href="/style/createEditAuthor.css">
</head>
<body>

<div class="form-container">
    <h2>Author Create</h2>
    <hr style="border: 1px solid #BEDCFE;">

    <form method="post" action="">
        <label for="first_name">First name</label>
        <input type="text" name="firstName" id="first_name"
               value="<?= htmlspecialchars($firstName ?? '') ?>">

        <?php if (!empty($errors['firstName'])): ?>
            <div class="error-msg" style="color: red;"><?= htmlspecialchars($errors['firstName']) ?></div>
        <?php endif; ?>

        <label for="last_name">Last name</label>
        <input type="text" name="lastName" id="last_name" value="<?= htmlspecialchars($lastName ?? '') ?>">

        <?php if (!empty($errors['lastName'])): ?>
            <div class="error-msg" style="color: red;"><?= htmlspecialchars($errors['lastName']) ?></div>
        <?php endif; ?>

        <div class="btn-wrapper">
            <button type="submit">Save</button>
        </div>

        <?php if (!empty($errors['general']) && empty($errors['firstName']) && empty($errors['lastName'])): ?>
            <div class="error-msg"
                 style="color: red; margin-top: 10px;"><?= htmlspecialchars($errors['general']) ?></div>
        <?php endif; ?>
    </form>

</div>

</body>
</html>
